<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Details</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
            background: #fafafa;
            color: #1a1a1a;
            line-height: 1.5;
        }
        
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 240px;
            height: 100vh;
            background: #ffffff;
            border-right: 1px solid #e5e7eb;
            padding: 2rem 0;
            z-index: 100;
        }
        
        .logo-section {
            padding: 0 1.5rem;
            margin-bottom: 3rem;
            border-bottom: 1px solid #f3f4f6;
            padding-bottom: 2rem;
        }
        
        .logo-section h1 {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 0.25rem;
        }
        
        .logo-section p {
            color: #6b7280;
            font-size: 0.75rem;
        }
        
        .nav-menu {
            list-style: none;
            padding: 0 1rem;
        }
        
        .nav-item {
            margin-bottom: 0.25rem;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            padding: 0.75rem 0.75rem;
            color: #6b7280;
            text-decoration: none;
            border-radius: 6px;
            font-size: 0.875rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }
        
        .nav-link:hover,
        .nav-link.active {
            color: #1a1a1a;
            background: #f3f4f6;
        }
        
        .nav-icon {
            width: 16px;
            height: 16px;
            margin-right: 0.75rem;
        }
        
        .main-content {
            margin-left: 240px;
            padding: 2rem;
            min-height: 100vh;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid #e5e7eb;
        }
        
        .header-title h1 {
            font-size: 1.875rem;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 0.25rem;
        }
        
        .header-title p {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .header-actions {
            display: flex;
            gap: 0.75rem;
        }
        
        .btn {
            padding: 0.75rem 1rem;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.875rem;
            transition: all 0.2s ease;
            border: none;
            cursor: pointer;
        }
        
        .btn-primary {
            background: #1a1a1a;
            color: #ffffff;
        }
        
        .btn-primary:hover {
            background: #374151;
        }
        
        .btn-secondary {
            background: #ffffff;
            color: #6b7280;
            border: 1px solid #e5e7eb;
        }
        
        .btn-secondary:hover {
            border-color: #d1d5db;
            color: #374151;
        }
        
        .content-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
            margin-bottom: 2rem;
        }
        
        .content-section {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            overflow: hidden;
        }
        
        .section-header {
            padding: 1.5rem;
            border-bottom: 1px solid #e5e7eb;
            background: #fafafa;
        }
        
        .section-header h2 {
            font-size: 1.125rem;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 0.25rem;
        }
        
        .section-header p {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .section-content {
            padding: 1.5rem;
        }
        
        .detail-grid {
            display: grid;
            gap: 1.5rem;
        }
        
        .detail-item {
            border-bottom: 1px solid #f3f4f6;
            padding-bottom: 1rem;
        }
        
        .detail-item:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }
        
        .detail-label {
            color: #6b7280;
            font-size: 0.75rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 0.5rem;
        }
        
        .detail-value {
            color: #1a1a1a;
            font-size: 0.875rem;
            word-wrap: break-word;
        }
        
        .detail-value.large {
            font-size: 1rem;
            font-weight: 500;
        }
        
        .message-content {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 6px;
            padding: 1rem;
            color: #374151;
            font-size: 0.875rem;
            line-height: 1.6;
            white-space: pre-wrap;
        }
        
        .status-badge {
            padding: 0.375rem 0.75rem;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            display: inline-block;
        }
        
        .status-new {
            background: #eff6ff;
            color: #1d4ed8;
        }
        
        .status-responded {
            background: #f0fdf4;
            color: #166534;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-label {
            display: block;
            color: #374151;
            font-weight: 500;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
        }
        
        .form-select, .form-textarea {
            width: 100%;
            background: #ffffff;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            color: #1a1a1a;
            font-size: 0.875rem;
            transition: border-color 0.2s ease;
        }
        
        .form-select {
            padding: 0.75rem;
        }
        
        .form-textarea {
            padding: 0.75rem;
            min-height: 120px;
            resize: vertical;
        }
        
        .form-select:focus, .form-textarea:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }
        
        .form-textarea::placeholder {
            color: #9ca3af;
        }
        
        .meta-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0.75rem 0;
            border-bottom: 1px solid #f3f4f6;
        }
        
        .meta-item:last-child {
            border-bottom: none;
        }
        
        .meta-label {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .meta-value {
            color: #1a1a1a;
            font-size: 0.875rem;
            font-weight: 500;
        }
        
        .success-message {
            background: #f0fdf4;
            border: 1px solid #bbf7d0;
            border-radius: 6px;
            padding: 0.75rem;
            margin-bottom: 1.5rem;
            color: #166534;
            font-size: 0.875rem;
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
            
            .main-content {
                margin-left: 0;
                padding: 1rem;
            }
            
            .content-grid {
                grid-template-columns: 1fr;
            }
            
            .header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            
            .header-actions {
                width: 100%;
                justify-content: flex-start;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo-section">
            <h1>Admin Portal</h1>
            <p>Portfolio Management</p>
        </div>
        
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/dashboard" class="nav-link">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"/>
                    </svg>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/submissions" class="nav-link active">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                    </svg>
                    Submissions
                </a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <div class="header-title">
                <h1>Submission Details</h1>
                <p>Review and manage contact form submission</p>
            </div>
            <div class="header-actions">
                <a href="/submissions" class="btn btn-secondary">← Back to List</a>
            </div>
        </div>

        <% if (message) { %>
            <div class="success-message">
                <%= message %>
            </div>
        <% } %>

        <!-- Content Grid -->
        <div class="content-grid">
            <!-- Main Details -->
            <div class="content-section">
                <div class="section-header">
                    <h2>Contact Information</h2>
                    <p>Details provided by the contact</p>
                </div>
                <div class="section-content">
                    <div class="detail-grid">
                        <div class="detail-item">
                            <div class="detail-label">Full Name</div>
                            <div class="detail-value large"><%= submission.name %></div>
                        </div>
                        
                        <div class="detail-item">
                            <div class="detail-label">Email Address</div>
                            <div class="detail-value">
                                <a href="mailto:<%= submission.email %>" style="color: #3b82f6; text-decoration: none;">
                                    <%= submission.email %>
                                </a>
                            </div>
                        </div>
                        
                        <% if (submission.company) { %>
                            <div class="detail-item">
                                <div class="detail-label">Company/Organization</div>
                                <div class="detail-value"><%= submission.company %></div>
                            </div>
                        <% } %>
                        
                        <div class="detail-item">
                            <div class="detail-label">Project Type</div>
                            <div class="detail-value"><%= submission.project_type %></div>
                        </div>
                        
                        <% if (submission.budget) { %>
                            <div class="detail-item">
                                <div class="detail-label">Budget</div>
                                <div class="detail-value"><%= submission.budget %></div>
                            </div>
                        <% } %>
                        
                        <% if (submission.contact_method) { %>
                            <div class="detail-item">
                                <div class="detail-label">Preferred Contact Method</div>
                                <div class="detail-value"><%= submission.contact_method %></div>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>

            <!-- Status & Actions -->
            <div class="content-section">
                <div class="section-header">
                    <h2>Status & Actions</h2>
                    <p>Manage submission status</p>
                </div>
                <div class="section-content">
                    <div class="detail-item">
                        <div class="detail-label">Current Status</div>
                        <div class="detail-value">
                            <span class="status-badge status-<%= submission.status %>">
                                <%= submission.status %>
                            </span>
                        </div>
                    </div>
                    
                    <form method="POST" action="/submission/<%= submission.id %>/update">
                        <div class="form-group">
                            <label for="status" class="form-label">Update Status</label>
                            <select id="status" name="status" class="form-select">
                                <option value="new" <%= submission.status === 'new' ? 'selected' : '' %>>New</option>
                                <option value="responded" <%= submission.status === 'responded' ? 'selected' : '' %>>Responded</option>
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="notes" class="form-label">Admin Notes</label>
                            <textarea 
                                id="notes" 
                                name="notes" 
                                class="form-textarea" 
                                placeholder="Add internal notes about this submission..."
                            ><%= submission.notes || '' %></textarea>
                        </div>
                        
                        <button type="submit" class="btn btn-primary">Update Submission</button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Message Content -->
        <div class="content-section">
            <div class="section-header">
                <h2>Message</h2>
                <p>Content of the contact form message</p>
            </div>
            <div class="section-content">
                <div class="message-content">
                    <%= submission.message || 'No message provided.' %>
                </div>
            </div>
        </div>

        <!-- Metadata -->
        <div class="content-section">
            <div class="section-header">
                <h2>Submission Metadata</h2>
                <p>Technical details about the submission</p>
            </div>
            <div class="section-content">
                <div class="meta-item">
                    <span class="meta-label">Submitted Date</span>
                    <span class="meta-value"><%= new Date(submission.created_at).toLocaleDateString() %></span>
                </div>
                <div class="meta-item">
                    <span class="meta-label">Submitted Time</span>
                    <span class="meta-value"><%= new Date(submission.created_at).toLocaleTimeString() %></span>
                </div>
                <% if (submission.ip_address) { %>
                    <div class="meta-item">
                        <span class="meta-label">IP Address</span>
                        <span class="meta-value"><%= submission.ip_address %></span>
                    </div>
                <% } %>
                <% if (submission.user_agent) { %>
                    <div class="meta-item">
                        <span class="meta-label">User Agent</span>
                        <span class="meta-value" style="font-size: 0.75rem; color: #6b7280;"><%= submission.user_agent %></span>
                    </div>
                <% } %>
                <div class="meta-item">
                    <span class="meta-label">Submission ID</span>
                    <span class="meta-value">#<%= submission.id %></span>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 