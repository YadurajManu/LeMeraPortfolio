<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submission Details - Admin Portal</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: #0a0a0a;
            color: #ffffff;
            line-height: 1.6;
        }
        
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 280px;
            height: 100vh;
            background: linear-gradient(180deg, #1a1a1a 0%, #0f0f0f 100%);
            border-right: 1px solid rgba(203, 69, 12, 0.2);
            padding: 2rem 0;
            z-index: 100;
        }
        
        .logo-section {
            padding: 0 2rem;
            margin-bottom: 3rem;
        }
        
        .logo-section h1 {
            font-size: 1.5rem;
            font-weight: 700;
            background: linear-gradient(135deg, #cb450c, #ff6b35);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 0.5rem;
        }
        
        .logo-section p {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.9rem;
        }
        
        .nav-menu {
            list-style: none;
        }
        
        .nav-item {
            margin-bottom: 0.5rem;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            padding: 0.875rem 2rem;
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: all 0.3s ease;
            border-right: 3px solid transparent;
        }
        
        .nav-link:hover,
        .nav-link.active {
            color: #ffffff;
            background: rgba(203, 69, 12, 0.1);
            border-right-color: #cb450c;
        }
        
        .nav-icon {
            width: 20px;
            height: 20px;
            margin-right: 1rem;
        }
        
        .main-content {
            margin-left: 280px;
            padding: 2rem;
            min-height: 100vh;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
            background: rgba(25, 25, 25, 0.8);
            backdrop-filter: blur(10px);
            padding: 1.5rem 2rem;
            border-radius: 16px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .header-title h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .header-title p {
            color: rgba(255, 255, 255, 0.6);
        }
        
        .header-actions {
            display: flex;
            gap: 1rem;
        }
        
        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            font-size: 0.9rem;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #cb450c, #ff6b35);
            color: white;
        }
        
        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.8);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .btn:hover {
            transform: translateY(-2px);
        }
        
        .submission-detail {
            background: rgba(25, 25, 25, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .detail-section {
            margin-bottom: 2rem;
        }
        
        .detail-section h3 {
            color: #cb450c;
            margin-bottom: 1rem;
            font-size: 1.2rem;
            border-bottom: 1px solid rgba(203, 69, 12, 0.3);
            padding-bottom: 0.5rem;
        }
        
        .detail-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
        }
        
        .detail-item {
            background: rgba(0, 0, 0, 0.3);
            padding: 1rem;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .detail-label {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.5rem;
        }
        
        .detail-value {
            color: #ffffff;
            font-size: 1rem;
            word-wrap: break-word;
        }
        
        .status-badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .status-new {
            background: rgba(59, 130, 246, 0.2);
            color: #93c5fd;
            border: 1px solid rgba(59, 130, 246, 0.3);
        }
        
        .status-responded {
            background: rgba(34, 197, 94, 0.2);
            color: #86efac;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }
        
        .message-content {
            background: rgba(0, 0, 0, 0.3);
            padding: 1.5rem;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            white-space: pre-wrap;
            line-height: 1.6;
        }
        
        .status-update-form {
            background: rgba(25, 25, 25, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 2rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
        }
        
        .form-select,
        .form-textarea {
            width: 100%;
            padding: 0.75rem;
            background: rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            color: #ffffff;
            font-size: 0.9rem;
        }
        
        .form-select:focus,
        .form-textarea:focus {
            outline: none;
            border-color: #cb450c;
        }
        
        .form-textarea {
            resize: vertical;
            min-height: 100px;
        }
        
        .alert {
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1rem;
        }
        
        .alert-success {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            color: #86efac;
        }
        
        .alert-error {
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.3);
            color: #fca5a5;
        }
        
        .logout-btn {
            background: rgba(220, 38, 38, 0.1);
            border: 1px solid rgba(220, 38, 38, 0.3);
            color: #fca5a5;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            text-decoration: none;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }
        
        .logout-btn:hover {
            background: rgba(220, 38, 38, 0.2);
            transform: translateY(-1px);
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo-section">
            <h1>YADURAJ</h1>
            <p>Admin Portal</p>
        </div>
        
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/dashboard" class="nav-link">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"/>
                    </svg>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/submissions" class="nav-link active">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                    </svg>
                    Submissions
                </a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <div class="header-title">
                <h1>Submission Details</h1>
                <p>View and manage individual submission</p>
            </div>
            <div class="header-actions">
                <a href="/submissions" class="btn btn-secondary">← Back to Submissions</a>
                <form method="POST" action="/logout" style="display: inline;">
                    <button type="submit" class="logout-btn">Logout</button>
                </form>
            </div>
        </div>

        <!-- Alert Messages -->
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>

        <!-- Submission Details -->
        <div class="submission-detail">
            <div class="detail-section">
                <h3>Contact Information</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Name</div>
                        <div class="detail-value"><%= submission.name %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Email</div>
                        <div class="detail-value"><%= submission.email %></div>
                    </div>
                    <% if (submission.company) { %>
                    <div class="detail-item">
                        <div class="detail-label">Company/Organization</div>
                        <div class="detail-value"><%= submission.company %></div>
                    </div>
                    <% } %>
                    <div class="detail-item">
                        <div class="detail-label">Contact Method</div>
                        <div class="detail-value"><%= submission.contact_method %></div>
                    </div>
                </div>
            </div>

            <div class="detail-section">
                <h3>Project Details</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Project Type</div>
                        <div class="detail-value"><%= submission.project_type %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Budget Range</div>
                        <div class="detail-value"><%= submission.budget %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Timeline</div>
                        <div class="detail-value"><%= submission.timeline %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">Status</div>
                        <div class="detail-value">
                            <span class="status-badge status-<%= submission.status %>">
                                <%= submission.status %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="detail-section">
                <h3>Message</h3>
                <div class="message-content"><%= submission.message %></div>
            </div>

            <div class="detail-section">
                <h3>Submission Info</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-label">Submitted</div>
                        <div class="detail-value"><%= new Date(submission.created_at).toLocaleString() %></div>
                    </div>
                    <% if (submission.responded_at) { %>
                    <div class="detail-item">
                        <div class="detail-label">Responded</div>
                        <div class="detail-value"><%= new Date(submission.responded_at).toLocaleString() %></div>
                    </div>
                    <% } %>
                    <div class="detail-item">
                        <div class="detail-label">IP Address</div>
                        <div class="detail-value"><%= submission.ip_address || 'N/A' %></div>
                    </div>
                    <div class="detail-item">
                        <div class="detail-label">User Agent</div>
                        <div class="detail-value"><%= submission.user_agent || 'N/A' %></div>
                    </div>
                </div>
            </div>

            <% if (submission.admin_notes) { %>
            <div class="detail-section">
                <h3>Admin Notes</h3>
                <div class="message-content"><%= submission.admin_notes %></div>
            </div>
            <% } %>
        </div>

        <!-- Status Update Form -->
        <div class="status-update-form">
            <h3 style="color: #cb450c; margin-bottom: 1.5rem;">Update Status</h3>
            <form method="POST" action="/submission/<%= submission.id %>/status">
                <div class="form-group">
                    <label for="status" class="form-label">Status</label>
                    <select id="status" name="status" class="form-select" required>
                        <option value="new" <%= submission.status === 'new' ? 'selected' : '' %>>New</option>
                        <option value="responded" <%= submission.status === 'responded' ? 'selected' : '' %>>Responded</option>
                    </select>
                </div>
                
                <div class="form-group">
                    <label for="notes" class="form-label">Admin Notes (Optional)</label>
                    <textarea id="notes" name="notes" class="form-textarea" placeholder="Add any internal notes about this submission..."><%= submission.admin_notes || '' %></textarea>
                </div>
                
                <button type="submit" class="btn btn-primary">Update Status</button>
            </form>
        </div>
    </div>
</body>
</html> 