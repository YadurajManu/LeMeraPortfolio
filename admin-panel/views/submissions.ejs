<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submissions</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
            background: #fafafa;
            color: #1a1a1a;
            line-height: 1.5;
        }
        
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 240px;
            height: 100vh;
            background: #ffffff;
            border-right: 1px solid #e5e7eb;
            padding: 2rem 0;
            z-index: 100;
        }
        
        .logo-section {
            padding: 0 1.5rem;
            margin-bottom: 3rem;
            border-bottom: 1px solid #f3f4f6;
            padding-bottom: 2rem;
        }
        
        .logo-section h1 {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 0.25rem;
        }
        
        .logo-section p {
            color: #6b7280;
            font-size: 0.75rem;
        }
        
        .nav-menu {
            list-style: none;
            padding: 0 1rem;
        }
        
        .nav-item {
            margin-bottom: 0.25rem;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            padding: 0.75rem 0.75rem;
            color: #6b7280;
            text-decoration: none;
            border-radius: 6px;
            font-size: 0.875rem;
            font-weight: 500;
            transition: all 0.2s ease;
        }
        
        .nav-link:hover,
        .nav-link.active {
            color: #1a1a1a;
            background: #f3f4f6;
        }
        
        .nav-icon {
            width: 16px;
            height: 16px;
            margin-right: 0.75rem;
        }
        
        .main-content {
            margin-left: 240px;
            padding: 2rem;
            min-height: 100vh;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid #e5e7eb;
        }
        
        .header-title h1 {
            font-size: 1.875rem;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 0.25rem;
        }
        
        .header-title p {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .filters-section {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .filters-grid {
            display: grid;
            grid-template-columns: 1fr auto auto;
            gap: 1rem;
            align-items: end;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
        }
        
        .form-label {
            color: #374151;
            font-weight: 500;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
        }
        
        .form-input, .form-select {
            background: #ffffff;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            color: #1a1a1a;
            padding: 0.75rem;
            font-size: 0.875rem;
            transition: border-color 0.2s ease;
        }
        
        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }
        
        .btn {
            background: #1a1a1a;
            border: none;
            border-radius: 6px;
            color: #ffffff;
            padding: 0.75rem 1.5rem;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn:hover {
            background: #374151;
        }
        
        .btn-secondary {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            color: #6b7280;
        }
        
        .btn-secondary:hover {
            border-color: #d1d5db;
            color: #374151;
        }
        
        .submissions-table {
            background: #ffffff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            overflow: hidden;
        }
        
        .table-header {
            background: #fafafa;
            padding: 1rem 1.5rem;
            border-bottom: 1px solid #e5e7eb;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .table-header h2 {
            font-size: 1.125rem;
            font-weight: 600;
            color: #1a1a1a;
        }
        
        .submissions-count {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .submissions-list {
            divide-y: 1px solid #f3f4f6;
        }
        
        .submission-row {
            display: grid;
            grid-template-columns: 2fr 1.5fr 1fr 1fr auto;
            gap: 1rem;
            padding: 1rem 1.5rem;
            align-items: center;
            transition: background-color 0.2s ease;
        }
        
        .submission-row:hover {
            background: #fafafa;
        }
        
        .submission-primary {
            font-weight: 600;
            color: #1a1a1a;
            font-size: 0.875rem;
            margin-bottom: 0.25rem;
        }
        
        .submission-secondary {
            color: #6b7280;
            font-size: 0.75rem;
        }
        
        .submission-meta {
            color: #6b7280;
            font-size: 0.75rem;
        }
        
        .status-badge {
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.625rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            text-align: center;
        }
        
        .status-new {
            background: #eff6ff;
            color: #1d4ed8;
        }
        
        .status-responded {
            background: #f0fdf4;
            color: #166534;
        }
        
        .actions {
            display: flex;
            gap: 0.5rem;
        }
        
        .btn-small {
            padding: 0.375rem 0.75rem;
            font-size: 0.75rem;
            border-radius: 4px;
        }
        
        .empty-state {
            text-align: center;
            padding: 3rem 1.5rem;
            color: #6b7280;
        }
        
        .empty-state h3 {
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
            color: #374151;
        }
        
        .empty-state p {
            font-size: 0.875rem;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            gap: 0.5rem;
            margin-top: 2rem;
        }
        
        .pagination-btn {
            padding: 0.5rem 0.75rem;
            border: 1px solid #e5e7eb;
            background: #ffffff;
            color: #6b7280;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.875rem;
            transition: all 0.2s ease;
        }
        
        .pagination-btn:hover {
            border-color: #d1d5db;
            color: #374151;
        }
        
        .pagination-btn.active {
            background: #1a1a1a;
            color: #ffffff;
            border-color: #1a1a1a;
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }
            
            .main-content {
                margin-left: 0;
                padding: 1rem;
            }
            
            .filters-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            
            .submission-row {
                grid-template-columns: 1fr;
                gap: 0.5rem;
            }
            
            .table-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo-section">
            <h1>Admin Portal</h1>
            <p>Portfolio Management</p>
        </div>
        
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/dashboard" class="nav-link">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"/>
                    </svg>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/submissions" class="nav-link active">
                    <svg class="nav-icon" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                    </svg>
                    Submissions
                </a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <div class="header-title">
                <h1>Submissions</h1>
                <p>Manage portfolio contact form submissions</p>
            </div>
        </div>

        <!-- Filters -->
        <div class="filters-section">
            <form method="GET" action="/submissions">
                <div class="filters-grid">
                    <div class="form-group">
                        <label for="search" class="form-label">Search</label>
                        <input 
                            type="text" 
                            id="search" 
                            name="search" 
                            class="form-input" 
                            placeholder="Search by name or email"
                            value="<%= search || '' %>"
                        >
                    </div>
                    <div class="form-group">
                        <label for="status" class="form-label">Status</label>
                        <select id="status" name="status" class="form-select">
                            <option value="">All Status</option>
                            <option value="new" <%= status === 'new' ? 'selected' : '' %>>New</option>
                            <option value="responded" <%= status === 'responded' ? 'selected' : '' %>>Responded</option>
                        </select>
                    </div>
                    <button type="submit" class="btn">Filter</button>
                </div>
            </form>
        </div>

        <!-- Submissions Table -->
        <div class="submissions-table">
            <div class="table-header">
                <h2>Submissions</h2>
                <span class="submissions-count"><%= totalSubmissions %> total</span>
            </div>
            
            <div class="submissions-list">
                <% if (submissions && submissions.length > 0) { %>
                    <% submissions.forEach(function(submission) { %>
                        <div class="submission-row">
                            <div>
                                <div class="submission-primary"><%= submission.name %></div>
                                <div class="submission-secondary"><%= submission.email %></div>
                            </div>
                            <div>
                                <div class="submission-primary"><%= submission.project_type %></div>
                                <div class="submission-secondary">
                                    <% if (submission.company) { %>
                                        <%= submission.company %>
                                    <% } else { %>
                                        No company
                                    <% } %>
                                </div>
                            </div>
                            <div class="submission-meta">
                                <%= new Date(submission.created_at).toLocaleDateString() %>
                            </div>
                            <div>
                                <span class="status-badge status-<%= submission.status %>">
                                    <%= submission.status %>
                                </span>
                            </div>
                            <div class="actions">
                                <a href="/submission/<%= submission.id %>" class="btn btn-secondary btn-small">View</a>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">
                        <h3>No submissions found</h3>
                        <p>No contact form submissions match your current filters.</p>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- Pagination -->
        <% if (totalPages > 1) { %>
            <div class="pagination">
                <% if (currentPage > 1) { %>
                    <a href="?page=<%= currentPage - 1 %>&search=<%= search || '' %>&status=<%= status || '' %>" class="pagination-btn">Previous</a>
                <% } %>
                
                <% for (let i = 1; i <= totalPages; i++) { %>
                    <a href="?page=<%= i %>&search=<%= search || '' %>&status=<%= status || '' %>" 
                       class="pagination-btn <%= i === currentPage ? 'active' : '' %>">
                        <%= i %>
                    </a>
                <% } %>
                
                <% if (currentPage < totalPages) { %>
                    <a href="?page=<%= currentPage + 1 %>&search=<%= search || '' %>&status=<%= status || '' %>" class="pagination-btn">Next</a>
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html> 